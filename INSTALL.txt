



DEPENDENCIES

zarith (Ocaml) --> sudo apt-get install libzarith-ocaml-dev
gmp (dev) --> sudo apt-get install libgmp-dev


Piplib : 

http://www.piplib.org/

en cas d'erreur de compil, installer autoconf, libconf, rm du libconf qui se situe dans le rép de compil puis faire autoreconf, qui rale, mais compiler quand même.
